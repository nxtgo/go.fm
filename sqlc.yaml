version: "2"

sql:
  - engine: "sqlite"
    schema: "db/sql/schema.sql"
    queries: "db/sql/queries.sql"
    database:
      uri: "file:database.db?_foreign_keys=on"
    gen:
      go:
        package: "db"
        out: "db"
        emit_prepared_queries: true
        emit_interface: false
